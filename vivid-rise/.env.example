# ===========================================
# Vite 프로젝트 → 반드시 VITE_ 접두사 사용
# (NEXT_PUBLIC_ / process.env 는 클라이언트에서 사용 불가)
# ===========================================

# 네이버 지도 (Result 페이지 지도 표시용)
# 코드에서 사용: import.meta.env.VITE_NAVER_MAP_CLIENT_ID (Vite 빌드 시점에 주입)
#
# [Client ID 확인 경로]
#   NCP 콘솔: https://console.ncloud.com/maps/application
#   메뉴: Service > Application Services > Maps > Application
#   → 등록한 Application 클릭 → 상세 화면에서 "Client ID" 값 복사
#   → Application 수정에서 "Dynamic Map" 사용 여부 및 "웹 서비스 URL" 확인
#
# 1) NCP "웹 서비스 URL"에 반드시 등록:
#      https://vivid-rise.vercel.app
#      http://localhost:5173
#
# 2) Vercel: Project > Settings > Environment Variables
#    - Name: VITE_NAVER_MAP_CLIENT_ID  /  Value: 위에서 복사한 Client ID
#    - 설정 후 재배포 필요 (빌드 시점에만 주입됨)
VITE_NAVER_MAP_CLIENT_ID=your-naver-map-client-id

# Supabase (대시보드 > Project Settings > API)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# 네이버 지역 검색 API (운세 추천 식당 → 실제 링크·사진, /api/naver-place 서버리스용)
# 로컬에서 vercel dev 쓸 때만 .env에 넣으면 됨. 배포는 Vercel 대시보드에서 설정.
# NAVER_CLIENT_ID=your-client-id
# NAVER_CLIENT_SECRET=your-client-secret
